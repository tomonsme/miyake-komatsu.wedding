[build]
  command = "npm run build"
  publish = "dist"

  # For Nuxt 4 + Nitro preset `netlify`, functions and routing
  # are configured automatically via `.netlify/functions-internal/server`.
  # No manual `functions` / `redirects` block is needed.
